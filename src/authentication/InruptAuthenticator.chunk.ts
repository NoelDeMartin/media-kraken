import { fetch, handleIncomingRedirect, login, logout } from '@inrupt/solid-client-authn-browser';

const InruptAuthClient = { fetch, handleIncomingRedirect, login, logout };

export default InruptAuthClient;
